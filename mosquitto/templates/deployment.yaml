apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mosquitto.fullname" . }}
  labels:
    {{- include "mosquitto.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "mosquitto.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ toJson .Values | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "mosquitto.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- if .Values.hostNetwork.enabled }}
      hostNetwork: true
      dnsPolicy: {{ .Values.hostNetwork.dnsPolicy }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            {{- range $name, $config := .Values.service.ports }}
            {{- if $config.enabled }}
            - name: {{ $name }}
              containerPort: {{ $config.targetPort }}
              protocol: {{ $config.protocol }}
              {{- if and $.Values.hostPorts.enabled (index $.Values.hostPorts $name) (index $.Values.hostPorts $name "port") }}
              hostPort: {{ index $.Values.hostPorts $name "port" }}
              {{- if index $.Values.hostPorts $name "hostIP" }}
              hostIP: {{ index $.Values.hostPorts $name "hostIP" }}
              {{- end }}
              {{- end }}
            {{- end }}
            {{- end }}
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            {{- if .Values.auth.secretRef.name }}
            - name: config
              mountPath: /mosquitto/config/mosquitto.conf
              subPath: mosquitto.conf
              readOnly: true
            {{- if .Values.auth.acls }}
            - name: config
              mountPath: /mosquitto/config/acl
              subPath: acl
              readOnly: true
            {{- end }}
            - name: auth-secret
              mountPath: /mosquitto/config/passwd
              subPath: {{ .Values.auth.secretRef.key | default "passwd" }}
              readOnly: true
            {{- else }}
            - name: config
              mountPath: /mosquitto/config
              readOnly: true
            {{- end }}
            {{- if .Values.persistence.enabled }}
            - name: data
              mountPath: /mosquitto/data
            {{- end }}
            {{- if and .Values.service.ports.mqttTls.enabled .Values.service.ports.mqttTls.tls.secretName }}
            - name: tls-certs
              mountPath: /mosquitto/certs
              readOnly: true
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "mosquitto.fullname" . }}-config
        {{- if .Values.auth.secretRef.name }}
        - name: auth-secret
          secret:
            secretName: {{ .Values.auth.secretRef.name }}
        {{- end }}
        {{- if .Values.persistence.enabled }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ .Values.persistence.existingClaim | default (printf "%s-data" (include "mosquitto.fullname" .)) }}
        {{- end }}
        {{- if and .Values.service.ports.mqttTls.enabled .Values.service.ports.mqttTls.tls.secretName }}
        - name: tls-certs
          secret:
            secretName: {{ .Values.service.ports.mqttTls.tls.secretName }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
